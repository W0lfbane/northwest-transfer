<% provide(:title, 'Groups') %>

<div class="">
    <ul class="list-unstyled">
      <% @groups.each do |group| %>
        <li>
          <%= group.name %>
        </li>
      <% end %>
    </ul>

    <div class="row">
        <div class="col-md-12 text-center">
            <%= paginate @groups %>
        </div>
    </div>
</div>
