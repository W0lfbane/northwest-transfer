<% provide(:title, 'Groups') %>

<div class="">
    <ul class="list-unstyled">
      <% @groups.each do |group| %>
        <li>
          <%= group.title %>
        </li>
      <% end %>
    </ul>
</div>
