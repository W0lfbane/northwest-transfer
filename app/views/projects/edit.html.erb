<% title('Editing Project titled ' + @project.title) %>
<% content_for :header_badge, render('customer') %>
<%= content_tag(:h1, 'Editing Project titled ' + @project.title) %>

<%= render partial: "form" %>