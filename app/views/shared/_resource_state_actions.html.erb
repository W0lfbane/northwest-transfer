<% object.fetch_events.map do |event| %>
    <%= button_to I18n.t(event).titleize, 
                    resource_state_change_path(object.class.name.downcase.pluralize, object), 
                    method: :patch, params: { status_method: event.to_s }, 
                    class: button_class %>
<% end %>
