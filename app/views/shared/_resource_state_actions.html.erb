<% object.aasm.events.map(&:name).map do |event| %>
   <%= button_to I18n.t(event), resource_state_change_path(object.class.name.downcase.pluralize, object), method: :patch, params: { status_method: event.to_s }, class: "form-control btn btn-default" %>
<% end %>
