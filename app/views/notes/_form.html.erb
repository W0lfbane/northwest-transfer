<%= form_for @note, url: @note.persisted? ? nested_note_path : notes_path do |f| %>
  <%= render 'shared/error_messages', object: f.object %>

  <%= render 'note_fields', f: f %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
