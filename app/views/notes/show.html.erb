<% title('Note by ' + @note.author) %>
<%= content_tag :h1, 'Note by ' + @note.author %>

<%= render 'note_attributes', note: @note %>

<%= link_to 'Edit', edit_nested_note_path(id: @note) %> |
<%= link_to 'Back', @resource %>
