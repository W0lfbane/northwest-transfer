<p id="notice"><%= notice %></p>

<p>
  <strong>Text:</strong>
  <%= @note.text %>
</p>

<p>
  <strong>Author:</strong>
  <%= @note.author %>
</p>

<%= link_to 'Edit', edit_note_path(@route_resource, @resource, @note) %> |
<%= link_to 'Back', notes_path(@route_resource, @resource) %>
