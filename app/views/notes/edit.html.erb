<% title('Editing Note by ' @note.author) %>
<%= content_tag :h1, 'Editing Note by ' @note.author %>

<%= render 'form', note: @note %>

<%= link_to 'Show', nested_note_path(id: @note) %> |
<%= link_to 'Back', @resource %>
