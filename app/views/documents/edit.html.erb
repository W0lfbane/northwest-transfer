<% title('Editing Document titled ' + @document.title) %>
<%= content_tag :h1, 'Editing Document titled ' + @document.title %>

<%= render 'form', document: @document %>

<%= link_to 'Show', flexible_resource_path(:document_path, @document) %> |
<%= link_to 'Back', @resource %>
