<div class="well background-lighter-grey">
  <%= form_for @document, url: @document.persisted? ? flexible_resource_path(document, :document_path) : documents_path do |f| %>
    <%= render 'shared/error_messages', object: @document %>

    <%= render 'document_fields', f: f %>
  
    <div class="actions">
      <%= f.submit %>
    </div>
  <% end %>
<div>